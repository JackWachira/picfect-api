language: python
python:
  - "2.7"

install:
  - pip install -r requirements.txt

env:
  - DB=sqlite

script:
  - python picfectapi/manage.py test --settings=picfectapi.settings.test

after_success:
  - coveralls